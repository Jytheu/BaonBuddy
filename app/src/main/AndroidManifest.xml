<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name=".utils.BaonBuddy"
        android:allowBackup="true"
        android:icon="@drawable/bbicon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/bbicon"
        android:supportsRtl="true"
        android:theme="@style/Theme.MaterialComponents.DayNight.NoActionBar">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".screens.login.LoginActivity" />
        <activity android:name=".screens.signup.SignupActivity" />
        <activity android:name=".screens.onboarding.OnboardingActivity" />
        <activity android:name=".screens.home.HomeActivity" />

        <activity android:name=".screens.history.HistoryActivity" />
        <activity android:name=".screens.profile.ProfileActivity" />
        <activity android:name=".screens.addlog.AddLogActivity" />
        <activity android:name=".screens.addlog.CategoryPickerActivity" />
        <activity android:name=".screens.amountinput.AmountInputActivity" />
    </application>
</manifest>